// Entry point — will be wired into index.html in Step 2.
// For now this file exists to validate the build toolchain.

import "./types";
import { loadConfig } from "./config";

export { loadConfig };
