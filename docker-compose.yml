services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: divemap
      POSTGRES_USER: divemap
      POSTGRES_PASSWORD: divemap_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
